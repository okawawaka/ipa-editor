# 機能検証チェックリスト (Feature Verification Checklist)

本ドキュメントは、「**IPA入力 国際音声字母エディタ**」に記載・宣伝されているすべての機能について、実装有無および動作仕様を検証・担保するための網羅的チェックリストです。

最終更新日: 2026年9月4日  
検証対象リポジトリ: [okawawaka/ipa-editor](https://github.com/okawawaka/ipa-editor)

---

## 📊 機能一覧と実装ステータス概要

| カテゴリ | 機能項目 | 仕様・動作内容 | 実装状態 |
| :--- | :--- | :--- | :---: |
| **入力制御** | 字母クリック入力 | 盤面の字母ボタンをクリックしてカーソル位置に挿入 | ✅ 実装済 |
| **入力制御** | 結合補助記号 | 字母の直後に結合用記号をクリックして合成 | ✅ 実装済 |
| **入力制御** | 1文字削除 (DELETE) | サロゲートペア・結合文字列を考慮した安全な1字削除 | ✅ 実装済 |
| **入力制御** | 全消去 (CLEAR) | 入力欄をクリアし、文字数カウンターをゼロにリセット | ✅ 実装済 |
| **入力制御** | **元に戻す (UNDO)** | 直前の入力・削除を取り消す (`UNDO` ボタン / `Ctrl+Z` / `Cmd+Z`) | ✅ 実装済 |
| **入力制御** | スペース挿入 (SPACE) | 空白文字を入力欄へ挿入 | ✅ 実装済 |
| **入力制御** | 精密表記 `[ ]` 囲み | 選択範囲または全体を `[` `]` で括る | ✅ 実装済 |
| **入力制御** | 音素表記 `/ /` 囲み | 選択範囲または全体を `/` `/` で括る | ✅ 実装済 |
| **入力制御** | サンプル挿入 (SAMPLE) | 代表的なIPA発音記号サンプルを挿入 | ✅ 実装済 |
| **クリップボード** | ワンクリックコピー (COPY) | `navigator.clipboard` によるコピー & トースト通知 | ✅ 実装済 |
| **表示・フィードバック** | 文字数カウンター | 結合文字列長（Unicode Code Points）を正確に表示 | ✅ 実装済 |
| **表示・フィードバック** | ホバーティッカー | マウスホバーで名称・英語名・調音分類をリアルタイム表示 | ✅ 実装済 |
| **表示・フィードバック** | スマートツールチップ | マウスカーソル追従型の詳細ポップアップ | ✅ 実装済 |
| **声調機能** | 単一トーンバー (55〜11) | `˥` 〜 `˩` の五段階レベルトーンバー入力 | ✅ 実装済 |
| **声調機能** | 声調合字 (Tone Ligatures) | 高下降 `˥˦` や上昇 `˩˥` などの折れ線グラフベクター表示 | ✅ 実装済 |
| **声調機能** | 入力欄での合字連結 | Noto Sans の OpenType 機能 (`liga`, `dlig`) による連結 | ✅ 実装済 |
| **字母網羅性** | 肺臓気流子音 | 破裂・鼻音・ふるえ・はじき・摩擦・側面摩擦・接近・側面接近 | ✅ 実装済 (全字母) |
| **字母網羅性** | 非肺臓気流子音 | 吸着音 (Clicks)、入破音 (Implosives)、放出音 (Ejectives) | ✅ 実装済 (全字母) |
| **字母網羅性** | 二重調音 | 接近音・破裂音・鼻音 (Double Articulation) | ✅ 実装済 (全字母) |
| **字母網羅性** | 母音台形 (Vowel Trapezoid) | 全28基本母音（舌高・前後・円唇/非円唇プロット） | ✅ 実装済 (全28種) |
| **字母網羅性** | 補助記号 (Diacritics) | 音節性・解放・発声・調音・同時調音・気流機構 | ✅ 実装済 (全記号) |
| **字母網羅性** | 超分節要素 & 声調 | 強勢・長短・境界・ピッチステップ・グローバル調 | ✅ 実装済 (全記号) |
| **UI / レイアウト** | スティッキードック | スクロール時も入力バーと操作盤が最上部に固定 | ✅ 実装済 |
| **UI / レイアウト** | スイス・スタイルデザイン | 1px黒線グリッド、角丸なし、不要な装飾の排除 | ✅ 実装済 |
| **ショートカット** | 操作説明モーダル表示 | キーボード `?` または `Shift` + `/` で開閉 | ✅ 実装済 |
| **ショートカット** | モーダル閉じる | `Esc` キーまたはオーバーレイクリックで閉じる | ✅ 実装済 |
| **ショートカット** | キーボードUNDO | `Ctrl` + `Z`（Mac: `Cmd` + `Z`）で元に戻す | ✅ 実装済 |
| **モバイル対応** | レスポンシブタブ切り替え | 画面幅768px以下でタブUIに切り替え | ✅ 実装済 |
| **モバイル対応** | テーブル横スワイプ | 幅広テーブル（子音・母音・声調・超分節）の横スクロール | ✅ 実装済 |
| **多言語対応 (i18n)** | 言語切り替えトグル | ヘッダーに「JA ｜ EN」ボタンを配置し瞬時に切り替え | ✅ 実装済 |
| **多言語対応 (i18n)** | 自動言語検出 | 日本語以外なら自動で英語表示、選択は永続化 | ✅ 実装済 |
| **多言語対応 (i18n)** | 盤面・解説の英語化 | 見出し・表軸・記号名称・ツールチップ・モーダルを網羅 | ✅ 実装済 |
| **公開・メタ情報** | ファビコン & アイコン | `favicon.svg`, `favicon.png`, `apple-touch-icon.png` | ✅ 実装済 |
| **公開・メタ情報** | SNSプレビュー (OGP) | `og-image.png` (1200x630) & Twitter Card | ✅ 実装済 |
| **公開・メタ情報** | GitHubリポジトリリンク | モーダル内に控えめなモノトーンバッジリンクを設置 | ✅ 実装済 |

---

## 🔍 詳細テスト手順書

### 1. UNDO（元に戻す）機能のテスト
- [ ] 字母ボタンを連続して数文字入力する。
- [ ] 操作バーの `UNDO` ボタンをクリックし、直前の文字が1つずつ消えることを確認する。
- [ ] キーボードで `Ctrl` + `Z`（Macは `Cmd` + `Z`）を押し、キー操作でも同様に直前の状態に戻ることを確認する。
- [ ] `CLEAR`（全消去）を押した後に `UNDO` を押し、消去前の文章が復元されることを確認する。
- [ ] `[ ]` や `/ /` で囲んだ後に `UNDO` を押し、囲み前の状態に戻ることを確認する。
- [ ] 履歴が空の状態で `UNDO` を押した際、「NOTHING TO UNDO」というトースト通知が表示されることを確認する。

### 2. クリップボード & トースト機能のテスト
- [ ] 入力欄に任意の文字列を入力し、`COPY` ボタンをクリックする。
- [ ] 画面下部に黒い帯で「COPIED TO CLIPBOARD」のトーストが表示され、約2秒後に自動でフェードアウトすることを確認する。
- [ ] メモ帳や別ウィンドウに `Ctrl` + `V` でペーストし、正確な文字列が貼り付けられることを確認する。
- [ ] 入力欄が空の状態で `COPY` を押し、「NO CONTENT TO COPY」が表示されることを確認する。

### 3. 表記囲み機能のテスト
- [ ] 入力欄にテキストがある状態で、一部の文字をマウスで範囲選択する。
- [ ] `[ ]` ボタンを押すと、選択した部分だけが `[` と `]` で括られることを確認する。
- [ ] 選択解除された状態で `/ /` ボタンを押すと、入力欄全体が `/` と `/` で括られることを確認する。

### 4. モバイル表示・レスポンシブ動作のテスト
- [ ] ブラウザの開発者ツールで画面幅を 375px（iPhone SEサイズ等）にする。
- [ ] 画面上部にタブ（「肺臓気流」「非肺臓・二重」「母音」「補助記号」「声調・超分節」）が表示されることを確認する。
- [ ] 「声調・超分節」タブをタップした際、テーブルが画面端で途切れず、左右にスワイプして「五段階声調」「曲線声調」「イントネーション」の全列が操作できることを確認する。
- [ ] 「補助記号」タブをタップした際も同様に全幅がスワイプ閲覧できることを確認する。

### 5. 操作説明モーダル & GitHubリンクのテスト
- [ ] ヘッダー右上の `HELP [ ? ]` ボタンをクリック、またはキーボードで `?` を押してモーダルが開くことを確認する。
- [ ] ショートカット一覧に `?`、`Esc` に加えて `Ctrl + Z` (UNDO) が記載されていることを確認する。
- [ ] 最下部の「GitHub: okawawaka/ipa-editor」リンクが目立ちすぎないモノトーンバッジ形式で表示されていることを確認する。
- [ ] リンクをクリックして別タブで [GitHubリポジトリ](https://github.com/okawawaka/ipa-editor) が開くことを確認する。
- [ ] `Esc` キーまたは枠外クリックでモーダルが閉じることを確認する。
