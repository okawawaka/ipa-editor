# GEMINI.md - ipa-editor 開発ガイドライン & 自律エージェント運用規則

本プロジェクト（`ipa-editor`）における Antigravity / AI エージェントの自律動作ルール、Git 運用規約、およびコード設計方針を定義します。

---

## 1. 自律実行（Agent Behavior: Always Allow）に関する方針

ユーザーがツールの自動承認（Always Allow）を有効化している前提で動作します。  
不要な中間確認を減らし、**「自律的な実装 → 自動検証（テスト・構文チェック） → 粒度の細かいコミット＆プッシュ」** を一気通貫で行います。

ただし、Git による巻き戻しを確実にするため、以下の**安全ルール**を厳格に遵守します。

### 🚨 絶対禁止事項（不可逆操作の禁止）
1. **破壊的 Git コマンドの禁止**:
   - `git push --force` (`-f`)
   - ユーザーの明示的な指示がない限り `git reset --hard` や `git clean -fd`
   - リモートブランチ・タグの削除
2. **無検証コミットの禁止**:
   - 構文エラー（SyntaxError）やアプリ破損のチェック（Node.js での読み込み検証など）を行わずにコミットしてはならない。
3. **スコープ外の改変禁止**:
   - ユーザーから指示された課題・機能追加に関係のないファイルの大規模リファクタリングやフォーマット変更を行わない。
4. **機密情報のコミット禁止**:
   - APIキー、トークン、個人情報、不要な一時ファイルを含めない。

---

## 2. プロジェクト概要・アーキテクチャ規約

- **目的**: 国際音声字母（IPA）の高速入力・学習支援 Web アプリケーション（[公開サイト](https://okawawaka.github.io/ipa-editor/)）。
- **技術スタック**:
  - **ピュア Vanilla HTML / CSS / JavaScript**（フレームワーク・ビルドツール不要）。
  - GitHub Pages による静的ホスティング（リポジトリルートを配信）。
- **デザイン哲学**:
  - **スイス・スタイル（国際タイポグラフィ様式）**:
    - `border-radius: 0`（角丸禁止、スクエアで統一）。
    - `box-shadow: none`（過度な立体影禁止、ハイコントラストな細線ボーダーを活用）。
    - ヘルベチカ・サンセリフ系の明瞭なタイポグラフィとグリッド配置。
    - 無駄な装飾やグラデーションを排除したミニマリズム。
- **データ整合性の保持**:
  - `js/ipa-data.js` の 339 種類の全記号データ、名称、説明、Unicode（サロゲートペア対応）の破損を絶対に起こさないこと。

---

## 3. Git コミット & プッシュ運用ルール

- **コミット粒度**: 1 つの機能・修正ごとにアトミック（最小単位）にコミットする。
- **コミットメッセージ**: [Conventional Commits](https://www.conventionalcommits.org/) 形式を採用する。
  - `feat:` 新機能追加
  - `fix:` バグ修正・表示崩れ修正
  - `docs:` ドキュメント（README、Wiki等）の変更
  - `style:` コードの意味に影響しない見た目・フォーマットの調整
  - `refactor:` リファクタリング
  - `test:` テストスクリプトの追加・更新
- **完了時のプッシュ**:
  - 指示されたタスクが完了し、テストがパスした段階で `origin/main` に `git push` を実行してリモートと同期する。

---

## 4. 自動検証プロトコル（品質保証）

コードを変更した際は、完了報告およびコミットを行う前に必ず以下を検証すること：
1. **構文・スクリプト検証**:
   - `node -c js/*.js` 等で JavaScript の構文エラーがないか確認。
   - `ipa-data.js` や `app.js` の主要関数（エディタ操作、履歴機能、キーボード描画）が正常にパースできることをスクリプトでテスト。
2. **レスポンシブ・表示の検証**:
   - スマートフォン（画面幅 375px〜420px 程度）での横スクロール崩れやモーダルのはみ出しがないか CSS ルール（Media Queries、`overflow-x: auto` など）を確認する。
3. **アンドゥ・リドゥ履歴の保証**:
   - テキスト入力・記号挿入・クリア操作時に `historyStack` が正しく更新される構造を維持する。

---

## 5. タスク遂行時のエージェント行動規範

- **自己解決・積極的推進**: 必要なファイル作成や修正は、許可待ちせず自律的に実行して結果を提示する。
- **透明性のある報告**: 完了時は「何を変更したか」「どのように検証したか」「どのコミットでプッシュしたか」を簡潔にまとめる。
- **ロールバック意識**: 万が一ユーザーが「元の状態に戻したい」と言った場合、直前のコミットハッシュから即座に復元できるよう履歴をクリーンに保つ。
